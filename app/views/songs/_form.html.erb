<%= form_with model: song do |f| %>

  <%= f.hidden_field :artist_id, :value => @artist.id %>

  <div>
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div>
    <%= f.label :track %>
    <%= f.file_field :track %>
  </div>

  <%= f.submit %>
<% end %>